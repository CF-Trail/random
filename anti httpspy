local cmds = {}
local __________________ = clonefunction
local cf = clonefunction(__________________)
local checking = true
local req = (syn and syn.request) or http and http.request or http_request or (fluxus and fluxus.request) or request

local function crash()
	while true do
		while true do
			
		end
	end
end

--// notDSF 1

local old
old = hookmetamethod(game, '__namecall', function(self, ...)
	local method = getnamecallmethod()
	local ischeckcaller = checkcaller()
	local args = {
		...
	}
	if self.IsA(self, 'BindableEvent') and method == 'Fire' and ischeckcaller then
		if typeof(args[1]) == 'table' and args[1].Url and checking then
			return crash()
		end
	end
	return old(self, ...)
end)

--// Anti Hook

if not cf or typeof(cf) ~= 'function' then
	return
end

local pcf = cf(pcall)

local suc, er = pcall(function()
	pcf(function()
		print('a')
	end)
end)

if not suc then
	return
end

--// notDSF 2

local chars = "abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ0123456789"
local result,result2 = "",""

for i = 1, 64 do
    local char = string.sub(chars, math.random(1, #chars), math.random(1, #chars))
    result = result .. char
end

for i = 1, 64 do
    local char = string.sub(chars, math.random(1, #chars), math.random(1, #chars))
    result2 = result2 .. char
end

local checktable = {
    ['webhookdiscord'] = {
        ['id'] = tostring(math.random() + 1),
        ['hash'] = result
    },
    ['guildedwebhook'] = {
        ['id'] = tostring(math.random() + 1),
        ['hash'] = result2
    }
}

pcf(function()
    req({
        Url = 'https://discord.com/api/webhooks/' .. checktable.webhookdiscord.id .. '/' .. checktable.webhookdiscord.hash,
        Method = 'POST',
        Headers = {
            ['Content-Type'] = 'application/json'
        },
        Body = game:GetService('HttpService'):JSONEncode(result)
    })
end)

pcf(function()
    req({
        Url = 'https://guilded.gg/api/webhooks/' .. checktable.guildedwebhook.id .. '/' .. checktable.guildedwebhook.hash,
        Method = 'POST',
        Headers = {
            ['Content-Type'] = 'application/json'
        },
        Body = game:GetService('HttpService'):JSONEncode(result)
    })
end)

checking = false

--// Actual commands

function cmds.HttpGet(...)
	local args = {
		...
	}
	return cf(game.HttpGetAsync)(game, unpack(args))
end

function cmds.request(...)
	return cf(req)(...)
end

return cmds
